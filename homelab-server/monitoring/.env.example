# Copy to .env and fill in values
# cp .env.example .env

# Get this from Beszel Hub UI when adding a new system
# Go to Hub → Add System → copy the public key shown
BESZEL_KEY=your_beszel_public_key_here

# Grafana admin password (change from default)
GRAFANA_PASSWORD=changeme
